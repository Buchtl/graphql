mutation create ($name: String, $product_no: [Int]) {
    createProduct(name: $name, product_no: $product_no) {
        id
        name
        product_no
        manufacturer {
            id
            name
        }
    }
}
